# Copy this file to .env and adjust values as needed
# These variables are optional. CLI flags override env values.
# Values support $VAR expansion and ~.

# Path to decrypted SQLite exported by signalbackup-tools.
# If not provided, the CLI/script will use: "$DB_OUT/signal_plain.sqlite".
DB=

# Base location for the decrypted SQLite file (directory).
# Defaults to "$HOME" if not set (HOME may default to project/.artifacts).
DB_OUT="$HOME"

# Path to Signal attachments.noindex
# macOS default shown here; adjust for your platform
SRC="$HOME/Library/Application Support/Signal/attachments.noindex"

# HTML output directory (index.html + assets/).
# Defaults to "$HOME/signal_browser_html" if not set.
HTML_OUT="$HOME/signal_browser_html"

# Desktop app vault root (encrypted archive). Optional.
# Default: ~/Library/Application Support/SignalBrowser  (macOS)
SIGNAL_BROWSER_HOME=

# OpenSSL binary (optional, unused by the current decryptor).
OPENSSL_BIN=
